box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - leipert/git-push@0.7.6:
         gh_oauth: $GIT_TOKEN
         basedir: public
         branch: gh-pages